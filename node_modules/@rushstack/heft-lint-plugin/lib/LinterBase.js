"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT license.
// See LICENSE in the project root for license information.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.LinterBase = void 0;
const path = __importStar(require("node:path"));
const node_perf_hooks_1 = require("node:perf_hooks");
const node_crypto_1 = require("node:crypto");
const node_core_library_1 = require("@rushstack/node-core-library");
class LinterBase {
    constructor(linterName, options) {
        this._fixesPossible = false;
        this._scopedLogger = options.scopedLogger;
        this._terminal = this._scopedLogger.terminal;
        this._buildFolderPath = options.buildFolderPath;
        this._buildMetadataFolderPath = options.buildMetadataFolderPath;
        this._linterConfigFilePath = options.linterConfigFilePath;
        this._linterName = linterName;
        this._fix = options.fix || false;
    }
    async performLintingAsync(options) {
        const startTime = node_perf_hooks_1.performance.now();
        let fileCount = 0;
        const commonDirectory = options.tsProgram.getCommonSourceDirectory();
        const relativePaths = new Map();
        const fileHash = (0, node_crypto_1.createHash)('md5');
        for (const file of options.typeScriptFilenames) {
            // Need to use relative paths to ensure portability.
            const relative = node_core_library_1.Path.convertToSlashes(path.relative(commonDirectory, file));
            relativePaths.set(file, relative);
            fileHash.update(relative);
        }
        const hashSuffix = fileHash.digest('base64').replace(/\+/g, '-').replace(/\//g, '_').slice(0, 8);
        const linterCacheVersion = await this.getCacheVersionAsync();
        const linterCacheFilePath = path.resolve(this._buildMetadataFolderPath, `_${this._linterName}-${hashSuffix}.json`);
        let linterCacheData;
        try {
            const cacheFileContent = await node_core_library_1.FileSystem.readFileAsync(linterCacheFilePath);
            if (cacheFileContent) {
                // Using JSON.parse instead of JsonFile because it is faster for plain JSON
                // This is safe because it is a machine-generated file that will not be edited by a human.
                // Also so that we can check for empty file first.
                linterCacheData = JSON.parse(cacheFileContent);
            }
        }
        catch (e) {
            if (node_core_library_1.FileSystem.isNotExistError(e)) {
                linterCacheData = undefined;
            }
            else if (e instanceof SyntaxError) {
                this._terminal.writeVerboseLine(`Error parsing ${linterCacheFilePath}: ${e}; ignoring cached data.`);
                linterCacheData = undefined;
            }
            else {
                throw e;
            }
        }
        const cachedNoFailureFileVersions = new Map((linterCacheData === null || linterCacheData === void 0 ? void 0 : linterCacheData.cacheVersion) === linterCacheVersion ? linterCacheData.fileVersions : []);
        const newNoFailureFileVersions = new Map();
        //#region Code from TSLint
        // Some of this code comes from here:
        // https://github.com/palantir/tslint/blob/24d29e421828348f616bf761adb3892bcdf51662/src/linter.ts#L161-L179
        // Modified to only lint files that have changed and that we care about
        const lintResults = [];
        for (const sourceFile of options.tsProgram.getSourceFiles()) {
            const filePath = sourceFile.fileName;
            const relative = relativePaths.get(filePath);
            if (relative === undefined || (await this.isFileExcludedAsync(filePath))) {
                continue;
            }
            const version = await this.getSourceFileHashAsync(sourceFile);
            const cachedVersion = cachedNoFailureFileVersions.get(relative) || '';
            if (cachedVersion === '' ||
                version === '' ||
                cachedVersion !== version ||
                options.changedFiles.has(sourceFile)) {
                fileCount++;
                const results = await this.lintFileAsync(sourceFile);
                if (results.length === 0) {
                    newNoFailureFileVersions.set(relative, version);
                }
                else {
                    for (const result of results) {
                        lintResults.push(result);
                    }
                }
            }
            else {
                newNoFailureFileVersions.set(relative, version);
            }
        }
        //#endregion
        await this.lintingFinishedAsync(lintResults);
        if (!this._fix && this._fixesPossible) {
            this._terminal.writeWarningLine('The linter reported that fixes are possible. To apply fixes, run Heft with the "--fix" option.');
        }
        const updatedTslintCacheData = {
            cacheVersion: linterCacheVersion,
            fileVersions: Array.from(newNoFailureFileVersions)
        };
        await node_core_library_1.JsonFile.saveAsync(updatedTslintCacheData, linterCacheFilePath, { ensureFolderExists: true });
        const duration = node_perf_hooks_1.performance.now() - startTime;
        this._terminal.writeVerboseLine(`Lint: ${duration}ms (${fileCount} files)`);
    }
    async getSourceFileHashAsync(sourceFile) {
        // TypeScript only computes the version during an incremental build.
        let version = sourceFile.version;
        if (!version) {
            // Compute the version from the source file content
            const sourceFileHash = (0, node_crypto_1.createHash)('sha1');
            sourceFileHash.update(sourceFile.text);
            version = sourceFileHash.digest('base64');
        }
        return version;
    }
}
exports.LinterBase = LinterBase;
//# sourceMappingURL=LinterBase.js.map