import type { HeftConfiguration, IHeftTaskSession, IHeftPlugin } from '@rushstack/heft';
import { type ISassConfiguration } from './SassProcessor';
export interface ISassConfigurationJson extends Partial<ISassConfiguration> {
}
export default class SassPlugin implements IHeftPlugin {
    private static _sassConfigurationLoader;
    private _sassConfiguration;
    private _sassProcessor;
    /**
     * Generate typings for Sass files before TypeScript compilation.
     */
    apply(taskSession: IHeftTaskSession, heftConfiguration: HeftConfiguration): void;
    private _runSassTypingsGeneratorAsync;
    private _loadSassProcessorAsync;
    private _loadSassConfigurationAsync;
}
//# sourceMappingURL=SassPlugin.d.ts.map