"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT license.
// See LICENSE in the project root for license information.
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
const path = __importStar(require("node:path"));
const node_core_library_1 = require("@rushstack/node-core-library");
const terminal_1 = require("@rushstack/terminal");
/**
 * This custom reporter presents Jest test results using Heft's logging system.
 *
 * @privateRemarks
 * After making changes to this code, it's recommended to use `--debug-heft-reporter` to compare
 * with the output from Jest's default reporter, to check our output is consistent with typical
 * Jest behavior.
 *
 * For reference, Jest's default implementation is here:
 * https://github.com/facebook/jest/blob/main/packages/jest-reporters/src/default_reporter.ts
 */
class HeftJestReporter {
    constructor(jestConfig, options) {
        this._terminal = options.logger.terminal;
        this._buildFolderPath = options.heftConfiguration.buildFolderPath;
        this._debugMode = options.debugMode;
    }
    // eslint-disable-next-line @typescript-eslint/naming-convention
    async onTestStart(test) {
        this._terminal.writeLine(terminal_1.Colorize.whiteBackground(terminal_1.Colorize.black('START')), ` ${this._getTestPath(test.path)}`);
    }
    // eslint-disable-next-line @typescript-eslint/naming-convention
    async onTestResult(test, testResult, aggregatedResult) {
        this._writeConsoleOutput(testResult);
        const { numPassingTests, numFailingTests, failureMessage, testExecError, perfStats, memoryUsage, snapshot: { updated: updatedSnapshots, added: addedSnapshots, unchecked: uncheckedSnapshots } } = testResult;
        // Calculate the suite duration time from the test result. This is necessary because Jest doesn't
        // provide the duration on the 'test' object (at least not as of Jest 25), and other reporters
        // (ex. jest-junit) only use perfStats:
        // https://github.com/jest-community/jest-junit/blob/12da1a20217a9b6f30858013175319c1256f5b15/utils/buildJsonResults.js#L112
        const duration = perfStats ? `${((perfStats.end - perfStats.start) / 1000).toFixed(3)}s` : '?';
        // calculate memoryUsage to MB reference -> https://jestjs.io/docs/cli#--logheapusage
        const memUsage = memoryUsage ? `, ${Math.floor(memoryUsage / 1000000)}MB heap size` : '';
        const message = ` ${this._getTestPath(test.path)} ` +
            `(duration: ${duration}, ${numPassingTests} passed, ${numFailingTests} failed${memUsage})`;
        if (numFailingTests > 0) {
            this._terminal.writeLine(terminal_1.Colorize.redBackground(terminal_1.Colorize.black('FAIL')), message);
        }
        else if (testExecError) {
            this._terminal.writeLine(terminal_1.Colorize.redBackground(terminal_1.Colorize.black(`FAIL (${testExecError.type})`)), message);
        }
        else {
            this._terminal.writeLine(terminal_1.Colorize.greenBackground(terminal_1.Colorize.black('PASS')), message);
        }
        if (failureMessage) {
            this._terminal.writeErrorLine(failureMessage);
        }
        if (updatedSnapshots) {
            this._terminal.writeErrorLine(`Updated ${this._formatWithPlural(updatedSnapshots, 'snapshot', 'snapshots')}`);
        }
        if (addedSnapshots) {
            this._terminal.writeErrorLine(`Added ${this._formatWithPlural(addedSnapshots, 'snapshot', 'snapshots')}`);
        }
        if (uncheckedSnapshots) {
            this._terminal.writeWarningLine(`${this._formatWithPlural(uncheckedSnapshots, 'snapshot was', 'snapshots were')} not checked`);
        }
    }
    // Tests often write messy console output.  For example, it may contain messages such as
    // "ERROR: Test successfully threw an exception!", which may confuse someone who is investigating
    // a build failure and searching its log output for errors.  To reduce confusion, we add a prefix
    // like "|console.error|" to each output line, to clearly distinguish test logging from regular
    // task output.  You can suppress test logging entirely using the "--silent" CLI parameter.
    _writeConsoleOutput(testResult) {
        if (testResult.console) {
            for (const logEntry of testResult.console) {
                switch (logEntry.type) {
                    case 'debug':
                        this._writeConsoleOutputWithLabel('console.debug', logEntry.message);
                        break;
                    case 'log':
                        this._writeConsoleOutputWithLabel('console.log', logEntry.message);
                        break;
                    case 'warn':
                        this._writeConsoleOutputWithLabel('console.warn', logEntry.message);
                        break;
                    case 'error':
                        this._writeConsoleOutputWithLabel('console.error', logEntry.message);
                        break;
                    case 'info':
                        this._writeConsoleOutputWithLabel('console.info', logEntry.message);
                        break;
                    case 'groupCollapsed':
                        if (this._debugMode) {
                            // The "groupCollapsed" name is too long
                            this._writeConsoleOutputWithLabel('collapsed', logEntry.message);
                        }
                        break;
                    case 'assert':
                    case 'count':
                    case 'dir':
                    case 'dirxml':
                    case 'group':
                    case 'time':
                        if (this._debugMode) {
                            this._writeConsoleOutputWithLabel(logEntry.type, `(${logEntry.type}) ${logEntry.message}`, true);
                        }
                        break;
                    default:
                        // Let's trap any new log types that get introduced in the future to make sure we handle
                        // them correctly.
                        throw new node_core_library_1.InternalError('Unimplemented Jest console log entry type: ' + logEntry.type);
                }
            }
        }
    }
    _writeConsoleOutputWithLabel(label, message, debug) {
        if (message === '') {
            return;
        }
        const scrubbedMessage = node_core_library_1.Text.ensureTrailingNewline(node_core_library_1.Text.convertToLf(message));
        const lines = scrubbedMessage.split('\n').slice(0, -1);
        const PAD_LENGTH = 13; // "console.error" is the longest label
        const paddedLabel = '|' + label.padStart(PAD_LENGTH) + '|';
        const prefix = debug ? terminal_1.Colorize.yellow(paddedLabel) : terminal_1.Colorize.cyan(paddedLabel);
        for (const line of lines) {
            this._terminal.writeLine(prefix, ' ' + line);
        }
    }
    // eslint-disable-next-line @typescript-eslint/naming-convention
    async onRunStart(aggregatedResult, options) {
        // Jest prints some text that changes the console's color without a newline, so we reset the console's color here
        // and print a newline.
        this._terminal.writeLine('\u001b[0m');
        this._terminal.writeLine(`Run start. ${this._formatWithPlural(aggregatedResult.numTotalTestSuites, 'test suite', 'test suites')}`);
    }
    // eslint-disable-next-line @typescript-eslint/naming-convention
    async onRunComplete(contexts, results) {
        const { numPassedTests, numFailedTests, numTotalTests, numRuntimeErrorTestSuites, snapshot: { uncheckedKeysByFile: uncheckedSnapshotsByFile } } = results;
        this._terminal.writeLine();
        this._terminal.writeLine('Tests finished:');
        const successesText = `  Successes: ${numPassedTests}`;
        this._terminal.writeLine(numPassedTests > 0 ? terminal_1.Colorize.green(successesText) : successesText);
        const failText = `  Failures: ${numFailedTests}`;
        this._terminal.writeLine(numFailedTests > 0 ? terminal_1.Colorize.red(failText) : failText);
        if (numRuntimeErrorTestSuites) {
            this._terminal.writeLine(terminal_1.Colorize.red(`  Failed test suites: ${numRuntimeErrorTestSuites}`));
        }
        if (uncheckedSnapshotsByFile.length > 0) {
            this._terminal.writeWarningLine(`  Test suites with unchecked snapshots: ${uncheckedSnapshotsByFile.length}`);
        }
        this._terminal.writeLine(`  Total: ${numTotalTests}`);
    }
    getLastError() {
        // This reporter doesn't have any errors to throw
    }
    _getTestPath(fullTestPath) {
        return path.relative(this._buildFolderPath, fullTestPath);
    }
    _formatWithPlural(num, singular, plural) {
        return `${num} ${num === 1 ? singular : plural}`;
    }
}
exports.default = HeftJestReporter;
//# sourceMappingURL=HeftJestReporter.js.map