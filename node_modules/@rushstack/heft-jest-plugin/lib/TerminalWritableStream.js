"use strict";
// Copyright (c) Microsoft Corporation. All rights reserved. Licensed under the MIT license.
// See LICENSE in the project root for license information.
Object.defineProperty(exports, "__esModule", { value: true });
exports.TerminalWritableStream = void 0;
const node_stream_1 = require("node:stream");
// Regex to filter out screen clearing directives
// Can't use the AnsiEscape.removeCodes() function from node-core-library because we are only
// removing the clear screen directives, but want to preserve coloring.
// eslint-disable-next-line no-control-regex
const FILTER_REGEX = /\x1B\[2J\x1B\[0f|\x1B\[2J\x1B\[3J\x1B\[H/g;
class TerminalWritableStream extends node_stream_1.Writable {
    constructor(terminal) {
        super({
            objectMode: false,
            decodeStrings: false,
            defaultEncoding: 'utf-8'
        });
        this._terminal = terminal;
    }
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    _write(chunk, encoding, callback) {
        const stringified = typeof chunk === 'string' ? chunk : chunk.toString(encoding);
        const filtered = stringified.replace(FILTER_REGEX, '');
        this._terminal.write(filtered);
        callback();
    }
}
exports.TerminalWritableStream = TerminalWritableStream;
//# sourceMappingURL=TerminalWritableStream.js.map