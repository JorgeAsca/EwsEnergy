"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "default", {
    enumerable: true,
    get: function() {
        return WebpackServeConfigurationPlugin;
    }
});
const _PluginNames = require("../PluginNames");
const _SPFxDebugPageUrlUtilities = require("./SPFxDebugPageUrlUtilities");
function _define_property(obj, key, value) {
    if (key in obj) {
        Object.defineProperty(obj, key, {
            value: value,
            enumerable: true,
            configurable: true,
            writable: true
        });
    } else {
        obj[key] = value;
    }
    return obj;
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const WEBPACK5_PLUGIN_NAME = 'webpack5-plugin';
class WebpackServeConfigurationPlugin {
    get accessor() {
        if (!this._accessor) {
            this._accessor = {
                properties: {}
            };
        }
        return this._accessor;
    }
    /**
   * Generate the Webpack Dev Server configuration for SPFx component development
   */ apply(taskSession, heftConfiguration) {
        taskSession.requestAccessToPluginByName('@rushstack/heft-webpack5-plugin', WEBPACK5_PLUGIN_NAME, (accessor)=>{
            if (taskSession.parameters.watch && accessor.parameters.isServeMode) {
                accessor.hooks.onConfigure.tapPromise(_PluginNames.SpfxWebpackServePlugin, async (config)=>{
                    const additionalServePaths = await this.accessor.properties.getAdditionalServePathsAsync?.();
                    const { updateServeConfigAsync } = await Promise.resolve().then(()=>/*#__PURE__*/ _interop_require_wildcard(require("./updateServeConfigAsync")));
                    const noBrowser = taskSession.parameters.getFlagParameter(_SPFxDebugPageUrlUtilities.SPFxDebugPageUrlUtilities.NO_BROWSER_CLI_PARAMETER).value;
                    const serveConfigName = taskSession.parameters.getStringParameter(_SPFxDebugPageUrlUtilities.SPFxDebugPageUrlUtilities.CONFIG_CLI_HEFT_PARAMETER).value;
                    return updateServeConfigAsync(taskSession, heftConfiguration, additionalServePaths, config, noBrowser, serveConfigName);
                });
            }
        });
    }
    constructor(){
        _define_property(this, "_accessor", void 0);
    }
}

//# sourceMappingURL=./WebpackServeConfigurationPlugin.js.map