import crypto from 'node:crypto';
/**
 * @internal
 */ export function getFullHash(data, salt) {
    const hash = crypto.createHash('md5');
    if (salt !== undefined) {
        hash.update(salt, 'utf-8');
    }
    return hash.update(data).digest('hex');
}

//# sourceMappingURL=./getFullHash.js.map