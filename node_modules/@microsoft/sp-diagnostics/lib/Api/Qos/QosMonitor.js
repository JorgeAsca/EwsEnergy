import { _BrowserUtilities, _SPKillSwitch } from '@microsoft/sp-core-library';
// Copyright (c) Microsoft. All rights reserved.
import { Guid, Text, Validate } from '@microsoft/sp-core-library';
import { getErrorMessage, getQosEndSchemaFromError, CustomError } from '@msinternal/odsp-core-bundle';
import TraceLogger from '../Trace/TraceLogger';
import { QosResultType } from './QosResultType';
import QosLogger from './QosLogger';
import EngagementLogger from '../Engagement/EngagementLogger';
import Diagnostics from '../../Diagnostics';
import DiagnosticsSettingsManager from '../../DiagnosticsSettingsManager';
import { merge } from '@microsoft/sp-lodash-subset';
var LOG_SOURCE = {
    id: 'QosMonitor'
};
var QOS_MONISTOR_HAS_ENDED_ERROR_STRING = "QoS monitor '{0}' has ended with '{1}' already. Ignoring '{2}' tag.";
var AUTHORING_SESSION_ID_KEY = 'AuthoringSessionId';
var AUTHORING_OP_SEQUENCE_ID_KEY = 'AuthoringOpSequenceId';
var AUTHORING_SESSION_CLIENT_COUNT = 'AuthoringSessionClientCount';
/**
 * Qos logger
 * This class represents the Qos monitor APIs
 *
 * @internal
 */
var QosMonitor = /** @class */ (function () {
    /**
     * Creates a QoS monitor (initializes it and logs the start event)
     * @param scenarioName - Unique name of the QoS scenario
     * You will see start tag: SPPages.ScenarioName.Start
     * @param copyToCosmos - Indicate whether this data should be used as Engagement as well, and sent to COSMOS.
     * @param copyToInterana - Indicate whether this data should be used as Engagement as well, and sent to Interana.
     */
    function QosMonitor(scenarioName, copyToCosmos, copyToInterana) {
        var _a;
        if (copyToCosmos === void 0) { copyToCosmos = false; }
        if (copyToInterana === void 0) { copyToInterana = false; }
        var _this = this;
        this._qosResultTypeValue = (_a = {},
            _a[QosResultType.Success] = 'Success',
            _a[QosResultType.Failure] = 'Failure',
            _a[QosResultType.ExpectedFailure] = 'ExpectedFailure',
            _a);
        this._ensureAuthoringSessionId = function () {
            var _a, _b, _c, _d;
            var authoringSessionId = (_b = (_a = DiagnosticsSettingsManager.settings) === null || _a === void 0 ? void 0 : _a.extraDiagnosticInfo) === null || _b === void 0 ? void 0 : _b.authoringSessionId;
            if (authoringSessionId) {
                _this._ensureEndExtraData();
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                _this._endExtraData[AUTHORING_SESSION_ID_KEY] = authoringSessionId;
                var authoringOpSequenceId = (_d = (_c = DiagnosticsSettingsManager.settings) === null || _c === void 0 ? void 0 : _c.extraDiagnosticInfo) === null || _d === void 0 ? void 0 : _d.authoringOpSequenceId;
                if (authoringOpSequenceId) {
                    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                    _this._endExtraData[AUTHORING_OP_SEQUENCE_ID_KEY] = authoringOpSequenceId;
                }
                var authoringSessionClientCount = Diagnostics.getAuthoringSessionClientCountInfo();
                if (authoringSessionClientCount && _this._endExtraData) {
                    _this._endExtraData[AUTHORING_SESSION_CLIENT_COUNT] = authoringSessionClientCount;
                }
            }
        };
        Validate.isNonemptyString(scenarioName, 'scenarioName');
        this._id = Guid.newGuid().toString();
        if (!this._isDebugSession()) {
            QosLogger.instance.startQosMonitor(this._id, { name: scenarioName });
        }
        this._scenarioName = scenarioName;
        this._hasEnded = false;
        this._copyToCosmos = copyToCosmos;
        this._copyToInterana = copyToInterana;
    }
    QosMonitor.startRealTimeProcessing = function () {
        if (!QosLogger.instance.shouldProcessInRealTime) {
            this._shouldProcessInRealTime(true);
        }
    };
    /**
     * Only used in unit tests
     * @internal
     */
    QosMonitor._shouldProcessInRealTime = function (processInRealTime) {
        QosLogger.instance.shouldProcessInRealTime = processInRealTime;
    };
    Object.defineProperty(QosMonitor.prototype, "name", {
        get: function () {
            return this._scenarioName;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(QosMonitor.prototype, "shortEventName", {
        get: function () {
            return 'Qos';
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(QosMonitor.prototype, "resultCode", {
        get: function () {
            return this._endResultCode;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(QosMonitor.prototype, "resultType", {
        get: function () {
            return this._endResult;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(QosMonitor.prototype, "extraData", {
        get: function () {
            return this._endExtraData;
        },
        set: function (value) {
            this._endExtraData = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(QosMonitor.prototype, "hasEnded", {
        get: function () {
            return this._hasEnded;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * QoS monitor ends with success
     * You will see success tag: SPPages.ScenarioName.Success
     */
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    QosMonitor.prototype.writeSuccess = function (extraData) {
        this._writeQosEnd(QosResultType.Success, undefined, undefined, extraData);
    };
    /**
     * QoS monitor ends with failure
     * You will see failure tag: SPPages.ScenarioName.<failuretag>.Failure.
     * <failuretag> will be filled with param tagName
     */
    QosMonitor.prototype.writeUnexpectedFailure = function (tagNameSuffix, ex, 
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    extraData) {
        if (tagNameSuffix === void 0) { tagNameSuffix = 'DefaultUnexpected'; }
        this._writeQosEnd(QosResultType.Failure, tagNameSuffix, ex, extraData);
    };
    /*
     * QoS monitor ends with expected failure
     * You will see success tag: ModernPublish.RenderPublishPage.<failuretag>.ExpectedFailure.
     * <failuretag> will be filled with param tagName
     */
    QosMonitor.prototype.writeExpectedFailure = function (tagNameSuffix, ex, 
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    extraData) {
        if (tagNameSuffix === void 0) { tagNameSuffix = 'DefaultExpected'; }
        this._writeQosEnd(QosResultType.ExpectedFailure, tagNameSuffix, ex, extraData);
    };
    QosMonitor.prototype._writeQosEnd = function (resultType, tagNameSuffix, ex, 
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    extraData) {
        if (this._shouldSkip(resultType)) {
            return;
        }
        this._ensureCorrelationId(resultType, extraData);
        this._endResultCode = tagNameSuffix;
        this._endResult = resultType;
        var errorString = !ex ? undefined : getErrorMessage(ex);
        if (ex instanceof CustomError) {
            var endSchema = getQosEndSchemaFromError(ex);
            if (endSchema) {
                endSchema.resultType = this._endResult;
                // Make sure the extraData from `endSchema` takes priority so that
                // extra data like correlation id from the error object is logged
                // instead of the generic extra data from `_endExtraData`.
                endSchema.extraData = merge({}, this._endExtraData, endSchema.extraData);
            }
            QosLogger.instance.writeQosEndResult(this._id, endSchema);
        }
        else {
            var endData = {
                resultCode: this._endResultCode,
                resultType: this._endResult,
                error: errorString,
                extraData: this._endExtraData
            };
            QosLogger.instance.writeQosEndResult(this._id, endData);
        }
        this._writeToEngagement();
    };
    // eslint-disable-next-line @typescript-eslint/no-explicit-any
    QosMonitor.prototype._ensureCorrelationId = function (resultType, extraData) {
        var _this = this;
        if (extraData) {
            this._ensureEndExtraData();
            // Instead of slower ...[spread], copy over the incoming values manually
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            Object.keys(extraData).forEach(function (k) { return (_this._endExtraData[k] = extraData[k]); });
        }
        if (DiagnosticsSettingsManager.isInitialized && resultType !== QosResultType.Success) {
            var capitalKey = 'CorrelationId';
            var lowerKey = 'correlationId';
            var correlationId = ((this._endExtraData &&
                (this._endExtraData[capitalKey] || this._endExtraData[lowerKey])) ||
                DiagnosticsSettingsManager.settings.correlationId);
            if (correlationId) {
                if (!this._endExtraData) {
                    this._endExtraData = {};
                }
                this._endExtraData[capitalKey] = correlationId;
            }
        }
        this._logHostType();
        this._ensureAuthoringSessionId();
    };
    QosMonitor.prototype._ensureEndExtraData = function () {
        if (!this._endExtraData) {
            this._endExtraData = {};
        }
    };
    /**
     * Logs the application hosted in Teams in combination with which Teams application it was hosted on.
     * Viva1p-TeamsNative: Viva home app loaded through Teams Desktop application, not including mobile
     * Viva1p-TeamsWeb: Viva home app loaded through Teams Web browser, not including mobile
     * MEEBridge-TeamsNative: VivaConnections MEEBridge app loaded through Teams mobile client.
     * ClassHome-TeamsNative: ClassHome app loaded through Teams Desktop application, not including mobile
     * ClassHome-TeamsWeb: ClassHome app loaded through Teams Web browser, not including mobile
     */
    QosMonitor.prototype._logHostType = function () {
        if (DiagnosticsSettingsManager.isInitialized) {
            var appHostType = _BrowserUtilities.getTeamsAppType();
            // Skip logging for default SPO path for now to limit quota impact
            if (appHostType) {
                this._ensureEndExtraData();
                var hostKey = 'appHostType';
                // Unlikely, but avoid overwriting matching key value
                // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                if (!this._endExtraData[hostKey]) {
                    // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
                    this._endExtraData[hostKey] = appHostType;
                }
            }
        }
    };
    QosMonitor.prototype._isDebugSession = function () {
        try {
            if (!_SPKillSwitch.isActivated('761ccb86-4fa0-4f56-a05b-477bcc09bd9f' /* '08/01/2024', 'finnnavin: Let QoSMonitor run in debug, uploading is blocked at SDK-level.' */)) {
                return false;
            }
            else {
                return !!_BrowserUtilities.safelyAccessSessionStorage('spfx-debug');
            }
        }
        catch (_a) {
            // KillSwitches throw exceptions when not initialized.
            // Currently this only happens when spfx-debug is present, so telemetry isn't being logged by the handler, and we can return false.
            return false;
        }
    };
    QosMonitor.prototype._shouldSkip = function (result) {
        if (this._hasEnded && this._endResult !== undefined) {
            var message = Text.format(QOS_MONISTOR_HAS_ENDED_ERROR_STRING, this.name, this._qosResultTypeValue[this._endResult], this._qosResultTypeValue[result]);
            if (this._endResult !== result) {
                TraceLogger.logError(LOG_SOURCE, new Error(message));
            }
            if (DEBUG) {
                TraceLogger.logVerbose(LOG_SOURCE, message, 'skipEnd');
            }
            return true;
        }
        else {
            this._hasEnded = true;
            return this._isDebugSession();
        }
    };
    QosMonitor.prototype._writeToEngagement = function () {
        if (this._copyToInterana) {
            EngagementLogger.log({
                name: this.name,
                isIntentional: !!(this.extraData || {}).isIntentional,
                extraData: this.extraData || {}
            });
        }
        if (this._copyToCosmos) {
            // Engagement name requires certain format. Cannot just use QOS name.
            // Record Qos name in extra data 'qosName'
            // eslint-disable-next-line @typescript-eslint/no-explicit-any
            var engagementData = {
                name: 'Spfx.Qos.ToEngagement',
                extraData: this.extraData || {}
            };
            engagementData.extraData.qosName = this.name;
            if (this.resultType) {
                engagementData.extraData.qosResultType = this._qosResultTypeValue[this.resultType];
            }
            if (this.resultCode) {
                engagementData.extraData.qosResultCode = this.resultCode;
            }
            EngagementLogger.log(engagementData);
        }
    };
    return QosMonitor;
}());
export default QosMonitor;
//# sourceMappingURL=QosMonitor.js.map