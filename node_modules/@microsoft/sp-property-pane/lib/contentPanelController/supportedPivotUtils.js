import { SPPagesContentPanelPivot } from '@msinternal/sp-pages-content-panel-context';
import { isPageDetailsEnabled } from '../common/Flights';
import { getExcludedPanels } from './excludedPanels';
export function getSupportedPivots() {
    var excludedPanels = getExcludedPanels();
    return [SPPagesContentPanelPivot.configurationTools, SPPagesContentPanelPivot.pageDetails].filter(function (supportedPivots) { return !excludedPanels.has(supportedPivots); });
}
export function getPivotFromConsumerId(consumerId) {
    if (isPageDetailsEnabled() && consumerId === 'PageSettingsPane') {
        return SPPagesContentPanelPivot.pageDetails;
    }
    // for now, assume every other consumer belongs to configuration tools.
    // When other pivots are added, update the check above.
    return SPPagesContentPanelPivot.configurationTools;
}
//# sourceMappingURL=supportedPivotUtils.js.map