import * as React from 'react';
import { noop } from '@microsoft/sp-lodash-subset';
export function useActiveItemState(activeItem, defaultOpenItem) {
    var _a = React.useState(defaultOpenItem), internalActiveItem = _a[0], setInternalActiveItemId = _a[1];
    var isControlled = React.useState(function () { return activeItem !== undefined; })[0];
    return isControlled ? [activeItem, noop] : [internalActiveItem, setInternalActiveItemId];
}
//# sourceMappingURL=useActiveItemState.js.map