/// <reference types="react" />
import type { IThumbnail } from '../../../propertyPaneThumbnailPickerShared/PropertyPaneThumbnailPickerTypes';
export interface IIconPickerPreviewProps {
    /**
     * The thumbnail info being rendered.
     */
    thumbnail: IThumbnail | undefined;
    /**
     * Callback which is triggered when image is updated completely.
     */
    onItemChanged: (iconName: string) => void;
    /**
     * Whether to render the preview in full width as opposed to a square
     */
    previewFullWidth?: boolean;
    /**
     * Whether to render the preview with a gray background
     */
    previewGrayBackground?: boolean;
    /**
     * CSS class name to apply to the wrapper for the thumbnail renderer
     */
    thumbnailRendererWrapperClassName?: string;
    /**
     * CSS class name to apply to the icon in the thumbnail renderer
     */
    thumbnailRendererIconClassName?: string;
    /**
     * QoS prefix
     */
    qosPrefix?: string;
    /**
     * A custom set of icons to show in the icon picker. These will override the default fabric icons that appear.
     * The key is the icon display name and the value is the actual icon to render.
     */
    customIconSet?: Map<string, JSX.Element>;
    /**
     * If specifying a custom icon set, the name of the selected icon
     */
    customSelectedIconName?: string;
    /**
     * Callback which is triggered when delete image button is clicked.
     */
    onItemDeleted?: () => void;
    /**
     * Additional icons to inject into icon picker. These will be appended to the default fabric icons that appear.
     * The key is the icon display name and the value is the actual icon to render.
     */
    _extendedIcons?: Map<string, JSX.Element>;
    /**
     * If specified, disables the action buttons so a new Icon cannot be selected or changed.
     */
    disabled?: boolean;
}
//# sourceMappingURL=IIconPickerPreviewProps.d.ts.map