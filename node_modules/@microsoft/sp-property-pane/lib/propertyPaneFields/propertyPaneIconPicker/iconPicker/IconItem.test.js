import React from 'react';
import enzyme from 'enzyme';
import ReactSixteenAdapter from 'enzyme-adapter-react-16';
import { IconButton } from '@ms/office-ui-fabric-react-bundle';
import { IconItem } from './IconItem';
describe('Icon Item', function () {
    beforeAll(function () {
        enzyme.configure({ adapter: new ReactSixteenAdapter() });
    });
    it('should render IconButton and a div with IconName', function () {
        var iconItem = enzyme.shallow(React.createElement(IconItem, { isSelected: true, iconName: 'iconName', onClick: jest.fn(), index: 0, itemsCount: 1 }));
        expect(iconItem.find(IconButton)).toHaveLength(1);
        // Top level div should show iconName.
        expect(iconItem.find('div').at(0).text()).toBe('iconName');
    });
});
//# sourceMappingURL=IconItem.test.js.map