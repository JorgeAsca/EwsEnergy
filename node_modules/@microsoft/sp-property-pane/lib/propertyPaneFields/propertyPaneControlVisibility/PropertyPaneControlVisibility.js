import { __assign } from "tslib";
import { isWebPartHideOnMobileScreenReaderFixKSActivated } from '../../common/KillSwitches';
import { PropertyPaneFieldType } from '../../index';
import strings from '../../loc/Strings.resx';
// Note: Reference BaseWebPart.ts for the property GUID's source of truth.
var HIDE_ON_MOBILE_PROPERTY = '9caa98f0-9050-43f7-be06-9bc2bdd5e833';
/**
 * Creates control visibility-related fields to property pane.
 * @param properties - Data for initializing property pane fields.
 *
 * @internal
 */
export function PropertyPaneControlVisibility(properties) {
    var fields = [];
    // Toggle for visibility of control (web part or rte) in mobile / email view.
    fields.push({
        type: PropertyPaneFieldType.Toggle,
        targetProperty: properties.hideOnMobilePropertyName || HIDE_ON_MOBILE_PROPERTY,
        properties: __assign(__assign(__assign(__assign(__assign(__assign({ label: strings.ShowInMobileAndEmailToggleLabel }, (isWebPartHideOnMobileScreenReaderFixKSActivated() && {
            ariaLabel: strings.ShowInMobileAndEmailToggleLabel
        })), { offText: strings.ShowInMobileAndEmailToggleOffText }), (isWebPartHideOnMobileScreenReaderFixKSActivated() && {
            offAriaLabel: strings.ShowInMobileAndEmailToggleOffAriaLabel
        })), { onText: strings.ShowInMobileAndEmailToggleOnText }), (isWebPartHideOnMobileScreenReaderFixKSActivated() && {
            onAriaLabel: strings.ShowInMobileAndEmailToggleOnAriaLabel
        })), { dataAutomationId: 'controlShowInMobileAndEmailToggle', checked: !properties.hideOnMobile })
    });
    // Add future visibility-related fields here...
    return fields;
}
/**
 * Creates "Visibility" group (including fields defined above).
 * @param properties - Data for initializing property pane fields.
 *
 * @remarks
 * Note: We have this separate function to create a group so that developers can use the above fields for their own groups.
 *
 * @internal
 */
export function PropertyPaneControlVisibilityGroup(properties) {
    var _a;
    var group = {
        groupName: strings.ControlVisibilityGroupName,
        groupFields: []
    };
    (_a = group.groupFields).push.apply(_a, PropertyPaneControlVisibility(properties));
    return group;
}
//# sourceMappingURL=PropertyPaneControlVisibility.js.map