import type { IClientSideComponentManifest } from '@microsoft/sp-module-interfaces';
import type { ServiceScope } from '@microsoft/sp-core-library';
/**
 * Unique identification of the Teams App component.
 *
 * @internal
 */
export interface ITeamsAppComponentId {
    /**
     * Teams App ID.
     */
    appId: string;
    /**
     * For LOB apps - contains the id provided by a developer in the manifest.
     */
    externalAppId?: string;
    /**
     * Teams App component ID.
     */
    componentId: string;
}
/**
 * SPFx component that was synced from Teams.
 *
 * @internal
 */
export interface ITeamsAppComponent extends ITeamsAppComponentId {
    /**
     * App/component version.
     */
    version: string;
    /**
     * App name.
     */
    appName: string;
    /**
     * App description.
     */
    appDescription: string;
    /**
     * Group ID.
     */
    groupId: string;
    /**
     * Icon URL.
     */
    iconUrl?: string;
    /**
     * Office UI Fabric icon name.
     */
    officeUIFabricIconName?: string;
    /**
     * Component name.
     */
    name: string;
    /**
     * Component description.
     */
    description: string;
    /**
     * Connected bot id.
     */
    botId: string;
    /**
     * Ace default size.
     */
    defaultSize: string;
}
/**
 * The client for the Teams Apps sync.
 *
 * @internal
 */
export declare class TeamsAppsClient {
    private static _getTeamsAppComponentManifestsPromise;
    private static _ensureAppCatalogPromise;
    /**
     * Map[teamsAppId, Promise[Set[teamsAppId]]]
     *
     * We store a map where the same promise can be used for multiple entries.
     * In that case we can avoid multiple requests to the API for the same teamsAppId as well as avoid race conditions and allow to make extra requests if needed.
     */
    private readonly _appComponentsPermissionsPromises;
    private readonly _serviceScope;
    private _msGraphClientFactory;
    private _msGraphClientDeferred;
    constructor(serviceScope: ServiceScope);
    /**
     * Gets the list of Teams App components from Teams App Catalog.
     * @returns Promise that resolves to the list of Teams App components.
     */
    getTeamsAppComponentManifests(): Promise<IClientSideComponentManifest[]>;
    /**
     * Syncs the Teams App component into SharePoint App Catalog.
     */
    syncComponent(teamsAppComponentManifest: IClientSideComponentManifest): Promise<IClientSideComponentManifest>;
    /**
     * Requests permissions for the Teams App components (apps) for the current user.
     */
    requestPermissions(teamsAppComponentIds: ITeamsAppComponentId[]): Promise<void>;
    /**
     * Checks if the component is enabled for the current user based on requested Teams permissions.
     */
    isComponentEnabled(teamsAppId: string): Promise<boolean>;
    private _requestAppsPermissions;
    private _requestAppsPermissionsBatched;
    private _getCheckPermissionsUrl;
    private get _msGraphClient();
    private _teamsAppCatalogItemToAceManifests;
    private _convertBlobToBase64;
    private _teamsAppComponentToAceManifest;
}
//# sourceMappingURL=TeamsAppsClient.d.ts.map