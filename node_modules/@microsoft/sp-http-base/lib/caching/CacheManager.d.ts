import { type IRequestCacheOptions } from './IRequestCacheOptions';
import type { ICacheDataProvider } from './ICacheDataProvider';
import type { IClientCachableResponse } from './IClientCachableResponse';
import type { IPrefetchDataProvider } from '../prefetch/IPrefetchDataProvider';
/**
 * Cache Manager for http Requests
 *
 * @internal
 */
export declare class CacheManager {
    /**
     * Fetches latest data from server or cache. Updates the cache from server if needed.
     * @param cacheKey - The cache Key
     * @param cacheProvider - The cache Provider
     * @param cacheConfiguration - The Cache configuration
     * @param serverFetch - The server fetch call
     * @param prefetchProvider - The prefetch provider
     */
    static fetchAndCache<T, P extends Response>(cacheKey: any, cacheProvider: ICacheDataProvider<T>, cacheConfiguration: IRequestCacheOptions, serverFetch: () => Promise<P>, prefetchProvider?: IPrefetchDataProvider): Promise<T | P | IClientCachableResponse<T, P>>;
    private static _applyCacheThenNetworkStrategy;
    private static _getServerData;
    private static _applySimpleCacheThenNetworkStrategy;
    private static _applyCacheStrategy;
    private static _fetchAndUpdateCache;
    private static _updateCache;
}
//# sourceMappingURL=CacheManager.d.ts.map