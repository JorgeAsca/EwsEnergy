import { _SPEventManager } from '@microsoft/sp-core-library';
import { PageContext } from '@microsoft/sp-page-context';
import { BeforeFullPageRedirectEventArgs, PopupRequestEventArgs } from '../../ISPCookieRefresher';
import { _QosMonitor } from '@microsoft/sp-diagnostics';
import { FeatureOverrides } from '@msinternal/utilities-features';
var SPBrowserCookieRefresher = /** @class */ (function () {
    function SPBrowserCookieRefresher(serviceScope) {
        var _this = this;
        this._shouldCancelRedirect = false;
        this._showPopup = function () {
            var _a;
            if (!_this._popupWindow) {
                _this._popupWindow = window.open(_this._popupUrl, SPBrowserCookieRefresher._POPUP_WINDOW_TARGET, SPBrowserCookieRefresher._POPUP_WINDOW_CSS);
                if (_this._popupWindow) {
                    setTimeout(_this._popupClosedPoll, SPBrowserCookieRefresher._POPUP_POLL_INTERVAL);
                }
                else {
                    // this should be called from a user interaction which should NOT be blocked.  We can't do anything
                    // if it is
                    if (_this._cookieRefreshedPromiseReject) {
                        (_a = _this._refreshCookieQosMonitor) === null || _a === void 0 ? void 0 : _a.writeUnexpectedFailure('Popup blocked after popup request event');
                        _this._cookieRefreshedPromiseReject('Popup blocked after popup request event');
                    }
                }
            }
        };
        this._cancelRedirect = function () {
            _this._shouldCancelRedirect = true;
        };
        this._onMessageFromPopupHandler = function (event) {
            var _a, _b, _c, _d;
            try {
                var webUrl = new URL((_b = (_a = _this._pageContext) === null || _a === void 0 ? void 0 : _a.legacyPageContext) === null || _b === void 0 ? void 0 : _b.webAbsoluteUrl);
                if (event.origin !== webUrl.origin) {
                    return;
                }
                if (JSON.parse(event.data).MessageId === SPBrowserCookieRefresher._REAUTH_POPUP_SUCCESS_MESSAGE_ID) {
                    (_c = _this._popupWindow) === null || _c === void 0 ? void 0 : _c.close();
                    _this._popupWindow = null;
                    (_d = _this._refreshCookieQosMonitor) === null || _d === void 0 ? void 0 : _d.writeSuccess();
                    window.removeEventListener('message', _this._onMessageFromPopupHandler);
                    if (_this._cookieRefreshedPromiseResolve) {
                        _this._cookieRefreshedPromiseResolve();
                        // If we don't do this the timer will mark it as popup closed.
                        _this._cookieRefreshedPromiseReject = undefined;
                    }
                }
            }
            catch (err) {
                // We don't ever expect to hit this
                var messageHandlerQos = new _QosMonitor('SPBrowserCookieRefresher.messageHandlerException');
                messageHandlerQos.writeUnexpectedFailure(err);
            }
        };
        this._popupClosedPoll = function () {
            var _a;
            if (_this._popupWindow) {
                if (_this._popupWindow.closed) {
                    if (_this._cookieRefreshedPromiseReject) {
                        (_a = _this._refreshCookieQosMonitor) === null || _a === void 0 ? void 0 : _a.writeExpectedFailure('Popup closed by user');
                        _this._cookieRefreshedPromiseReject('Popup closed');
                    }
                }
                else {
                    setTimeout(_this._popupClosedPoll, SPBrowserCookieRefresher._POPUP_POLL_INTERVAL);
                }
            }
        };
        this._serviceScope = serviceScope;
        this._serviceScope.whenFinished(function () {
            _this._pageContext = _this._serviceScope.consume(PageContext.serviceKey);
        });
    }
    SPBrowserCookieRefresher.prototype.refreshCookie = function (claims, reauthId, usePopupAuth) {
        if (!this._refreshCookieQosMonitor) {
            this._refreshCookieQosMonitor = new _QosMonitor('SPBrowserCookieRefresher.refreshCookie');
        }
        if (usePopupAuth) {
            return this._refreshCookieViaPopup(claims, reauthId);
        }
        else {
            return this._refreshCookieViaFullPageRedirect(claims, reauthId);
        }
    };
    SPBrowserCookieRefresher.prototype._refreshCookieViaFullPageRedirect = function (claims, reauthId) {
        var _this = this;
        var _a, _b, _c;
        if (this._shouldDoFullPageRedirect()) {
            var fullPageRedirectUrl = this._getFullPageRedirectUrl(claims, reauthId);
            this._shouldCancelRedirect = false;
            _SPEventManager.instance.raiseEvent(SPBrowserCookieRefresher._onBeforeRedirectEventId, new BeforeFullPageRedirectEventArgs(fullPageRedirectUrl, this._cancelRedirect));
            if (!this._shouldCancelRedirect) {
                (_a = this._refreshCookieQosMonitor) === null || _a === void 0 ? void 0 : _a.writeSuccess();
                this._setFullPageRedirectTime();
                window.location.href = fullPageRedirectUrl;
                // This doens't really get hit but we want to add it incase something blocks the redirect
                // Nothing will resolve the promise in this case though
                return new Promise(function (resolve, reject) {
                    _this._cookieRefreshedPromiseReject = reject;
                    _this._cookieRefreshedPromiseResolve = resolve;
                });
            }
            else {
                (_b = this._refreshCookieQosMonitor) === null || _b === void 0 ? void 0 : _b.writeExpectedFailure('App cancelled redirect');
                return Promise.reject('Redirect being handled by application');
            }
        }
        else {
            (_c = this._refreshCookieQosMonitor) === null || _c === void 0 ? void 0 : _c.writeExpectedFailure('Redirect limit exceeded');
            return Promise.reject('Redirect limit exceeded');
        }
    };
    SPBrowserCookieRefresher.prototype._shouldDoFullPageRedirect = function () {
        var firstRedirectTime = this._getFullPageRedirectTime();
        if (firstRedirectTime) {
            var now = new Date();
            var firstRedirectDate = new Date(firstRedirectTime);
            return (now.getTime() - firstRedirectDate.getTime() >
                SPBrowserCookieRefresher._MIN_INTERVAL_TIME_FOR_ATTEMPTING_REDIRECT);
        }
        else {
            return true;
        }
    };
    SPBrowserCookieRefresher.prototype._getFullPageRedirectTime = function () {
        return window.sessionStorage.getItem(SPBrowserCookieRefresher._SPCOOKIEREFRESHER_REDIRECT_TIME_STORAGE_KEY);
    };
    SPBrowserCookieRefresher.prototype._setFullPageRedirectTime = function () {
        var currentDate = new Date();
        window.sessionStorage.setItem(SPBrowserCookieRefresher._SPCOOKIEREFRESHER_REDIRECT_TIME_STORAGE_KEY, currentDate.toString());
    };
    SPBrowserCookieRefresher.prototype._refreshCookieViaPopup = function (claims, reauthId) {
        var _this = this;
        this._popupUrl = this._getCookiePopupRefreshUrl(claims, reauthId);
        var returnPromise = new Promise(function (resolve, reject) {
            _this._cookieRefreshedPromiseReject = reject;
            _this._cookieRefreshedPromiseResolve = resolve;
        });
        window.addEventListener('message', this._onMessageFromPopupHandler);
        this._popupWindow = window.open(this._popupUrl, SPBrowserCookieRefresher._POPUP_WINDOW_TARGET, SPBrowserCookieRefresher._POPUP_WINDOW_CSS);
        // If the browser blocked the popupwindow it will be null
        if (this._popupWindow) {
            setTimeout(this._popupClosedPoll, SPBrowserCookieRefresher._POPUP_POLL_INTERVAL);
        }
        else {
            // We need to fire the event to request a popup and the calling code is expected to handle this
            _SPEventManager.instance.raiseEvent(SPBrowserCookieRefresher._onPopupRequestedEventId, new PopupRequestEventArgs(this._showPopup));
        }
        return returnPromise;
    };
    SPBrowserCookieRefresher.prototype._getFullPageRedirectUrl = function (claims, reauthId) {
        return this._getRefreshUrl(claims, reauthId, window.location.pathname + window.location.search);
    };
    SPBrowserCookieRefresher.prototype._getCookiePopupRefreshUrl = function (claims, reauthId) {
        return this._getRefreshUrl(claims, reauthId, SPBrowserCookieRefresher._POPUP_REFRESH_URL);
    };
    SPBrowserCookieRefresher.prototype._getRefreshUrl = function (claims, reauthId, returnUrl) {
        if (!this._pageContext ||
            !this._pageContext.legacyPageContext ||
            !this._pageContext.legacyPageContext.webAbsoluteUrl) {
            throw new Error('Page context is not properly initialized.');
        }
        var tenantDomain = new URL(this._pageContext.legacyPageContext.webAbsoluteUrl).origin;
        var baseUrl = new URL("".concat(tenantDomain).concat(SPBrowserCookieRefresher._REDIRECT_PATH));
        if (reauthId) {
            baseUrl.searchParams.append(SPBrowserCookieRefresher._REAUTH_QSP, reauthId);
        }
        else if (claims) {
            baseUrl.searchParams.append(SPBrowserCookieRefresher._CLAIMS_QSP, claims);
        }
        baseUrl.searchParams.append('ReturnUrl', "".concat(tenantDomain).concat(returnUrl));
        return baseUrl.toString();
    };
    SPBrowserCookieRefresher._REAUTH_POPUP_SUCCESS_MESSAGE_ID = 'CookieRefreshSuccess';
    SPBrowserCookieRefresher._POPUP_POLL_INTERVAL = 1000;
    SPBrowserCookieRefresher._MIN_INTERVAL_TIME_FOR_ATTEMPTING_REDIRECT = 2 * 60 * 1000; // 2 minutes
    SPBrowserCookieRefresher._SPCOOKIEREFRESHER_REDIRECT_TIME_STORAGE_KEY = 'spBrowserCookieRefresher.previousRedirectTime';
    SPBrowserCookieRefresher._POPUP_REFRESH_URL = "/_layouts/15/ReauthPopup.aspx?allowCallerToCloseWindow=true".concat(FeatureOverrides.CanOverride ? "&source=SilentCookieRefresher.silent&timeStamp=".concat(Date.now()) : '');
    SPBrowserCookieRefresher._REDIRECT_PATH = '/_forms/default.aspx';
    SPBrowserCookieRefresher._POPUP_WINDOW_CSS = 'height=600,width=483';
    SPBrowserCookieRefresher._POPUP_WINDOW_TARGET = '_blank';
    SPBrowserCookieRefresher._REAUTH_QSP = 'reauthid';
    SPBrowserCookieRefresher._CLAIMS_QSP = 'claims';
    SPBrowserCookieRefresher._onBeforeRedirectEventId = 'onBeforeFullPageRedirect';
    SPBrowserCookieRefresher._onPopupRequestedEventId = 'authPopup';
    return SPBrowserCookieRefresher;
}());
export { SPBrowserCookieRefresher };
//# sourceMappingURL=SPBrowserCookieRefresher.js.map