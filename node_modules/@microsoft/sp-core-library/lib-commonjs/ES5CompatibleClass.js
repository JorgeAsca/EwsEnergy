"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ES5CompatibleClass = void 0;
/* eslint-disable */
/**
 * Updates the ES6 class to be compatible with ES5.
 *
 * @internal
 */
function ES5CompatibleClass(es6Class) {
    //
    // override apply and call methods to support ES5 inheritance
    //
    es6Class.apply = function (thisArg, args) {
        var reflect = window.Reflect;
        // if the context of the apply is the ES6 class itself, then we use Reflect.construct to create the instance
        if (typeof reflect !== 'undefined' && reflect.construct) {
            if (this == es6Class) {
                return reflect.construct(es6Class, args, thisArg.constructor);
            }
        }
        // otherwise, we use the Function.apply method
        return Function.apply.apply(this, [thisArg, args]);
    };
    es6Class.call = function (thisArg) {
        var args = [];
        for (var _i = 1; _i < arguments.length; _i++) {
            args[_i - 1] = arguments[_i];
        }
        var reflect = window.Reflect;
        // if the context of the call is the ES6 class itself, then we use Reflect.construct to create the instance
        if (typeof reflect !== 'undefined' && reflect.construct) {
            if (this == es6Class) {
                return window.Reflect.construct(es6Class, args, thisArg.constructor);
            }
        }
        // otherwise, we use the Function.apply method
        return Function.apply.apply(this, [thisArg, args]);
    };
}
exports.ES5CompatibleClass = ES5CompatibleClass;
/* eslint-enable */
//# sourceMappingURL=ES5CompatibleClass.js.map