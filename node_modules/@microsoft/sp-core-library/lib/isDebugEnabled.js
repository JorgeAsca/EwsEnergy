import { BrowserUtilities } from './BrowserUtilities';
import SPFlight from './SPFlight';
/**
 * Specifies if debugging is enabled.
 *
 * @internal
 */
export function isDebugEnabled(queryParameters) {
    return (SPFlight.isDebugFlightEnabled &&
        SPFlight.isEnabled(1178 /* SppplatDebugExtension */) && // for debug extension
        (SPFlight.isEnabled(60787 /* APEXEnableDebugApplicationWithScoping */) || _isDebugModeOn(queryParameters)));
}
function _isDebugModeOn(queryParameters) {
    if (!queryParameters) {
        queryParameters = new URL(window.location.href).searchParams;
    }
    if (BrowserUtilities.safelyAccessSessionStorage('spfx-debug')) {
        // Don't load in TAB which monitor for unexpected files
        return !queryParameters.get('istabtest');
    }
    // Enable showing once ?debug is added as engineers typically do
    return !!queryParameters.get('debug');
}
//# sourceMappingURL=isDebugEnabled.js.map