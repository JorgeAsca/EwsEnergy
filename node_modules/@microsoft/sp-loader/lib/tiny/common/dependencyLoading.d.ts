import type { IClientSideComponentManifest, _IOperatorOrSwitch } from '@microsoft/sp-module-interfaces';
import type { IModuleLoader } from '../interfaces/IModuleLoader';
/**
 * Load all the component dependencies of an SPFx component.
 *
 * @param loader - Module loader instance.
 * @param manifest - Manifest of the dependent component.
 * @param depNames - Array of dependency names.
 *
 * @returns An array of Promises resolving to a respective dependency.
 */
export declare function loadComponentDependencies<T>(loader: IModuleLoader, manifest: IClientSideComponentManifest, depNames: string[]): Promise<T>[];
/**
 * Load all the path dependencies of an SPFx component.
 *
 * @param loader - Module loader instance.
 * @param manifest - Manifest of the dependent component.
 * @param depNames - Array of dependency names.
 *
 * @returns An array of Promises resolving to a respective dependency.
 */
export declare function loadPathDependencies<T>(loader: IModuleLoader, manifest: IClientSideComponentManifest, depNames: string[]): Promise<T>[];
export declare function _evaluateFlightOrManifestOperator(operator: _IOperatorOrSwitch): boolean;
//# sourceMappingURL=dependencyLoading.d.ts.map