import { __spreadArray } from "tslib";
import { Environment, EnvironmentType, Log, Session, _SPEventManager, _SPExperiment, _SPFlight, _SPKillSwitch } from '@microsoft/sp-core-library';
import { _Diagnostics, _LogHandler, _QosMonitor } from '@microsoft/sp-diagnostics';
import { _PerformanceLogger, _Telemetry } from '@msinternal/sp-telemetry';
import { AadTokenProvider, _AadConstants, _AadTokenProviders } from '@microsoft/sp-http-base';
import { DebugManager } from '../debug/DebugManager';
import { showBrowserDevToolsWarning } from '../DeveloperTools/BrowserDeveloperToolsWarning/showBrowserDevToolsWarning';
import * as developerToolsLoader from '../DeveloperTools/DeveloperToolsLoader';
import { APPLICATION_STARTED_DATA, REDIRECT_PARAM } from '../iframeLoadRedirector/IframeLoadRedirector';
import LocaleStore from '../stores/LocaleStore';
import ManifestStore from '../stores/ManifestStore';
import { createRootScope } from '../tiny/common/createRootScope';
import { SPComponentLoader } from '../tiny/componentLoader/SPComponentLoader';
import { RequireLoader } from '../tiny/requireLoader/RequireLoader';
import { OldManifestStoreShim } from '../tiny/store/ManifestStore';
import * as componentConstants from '../utilities/componentConstants';
import ResourceUrlChecker from '../utilities/ResourceUrlChecker';
import SPLoaderFlights from '../utilities/SPLoaderFlights';
import strings from './Starter.resx';
import { overrideXMLHttpRequest } from './overrideXMLHttpRequest';
import { overrideXMLHttpRequestKSActivated, overrideWorkloadKSActivated } from '../tiny/common/killSwitches';
import { isFindAppManifestInsteadUsingFirstKSActivated, isPassClientSideApplicationManifestToRootScopeKSActivated } from '../utilities/KillSwitches';
// !!! Show the developer tools warning as soon as the page loads !!!
if (!DEBUG && !DEPRECATED_UNIT_TEST) {
    showBrowserDevToolsWarning();
}
// Flight to enable refactor of flight/KS override bootstrapping into SPFlight and SPKillSwitch respectively,
// and setting cookies with feature overrides
var REFACTOR_DEBUG_FLIGHTS_AND_KILLSWITCHES_FLIGHT_ID = 61863;
var DEBUG_FLIGHTS_QUERY_PARAM = 'debugFlights';
var DEBUG_KILLSWITCHES_QUERY_PARAM = 'debugKillSwitches';
var ENABLE_FEATURES_QUERY_PARAM = 'enableFeatures';
var ENABLE_FEATURES_COOKIE_NAME = "FeatureOverrides_".concat(ENABLE_FEATURES_QUERY_PARAM);
var DISABLE_FEATURES_QUERY_PARAM = 'disableFeatures';
var DISABLE_FEATURES_COOKIE_NAME = "FeatureOverrides_".concat(DISABLE_FEATURES_QUERY_PARAM);
var COMPONENT_LOADER_GLOBAL_VARIABLE = '_spComponentLoader';
var EARLY_QOS_KS = '0f88ae27-bd27-471a-a18e-1b833134575e';
var DISABLE_TOKEN_FETCH_KS = '2bf3e1ce-55da-4552-bc41-e3d072202d3c';
var DO_NOT_ENSURE_GUEST_USERS_KS = '83f6380b-bbb5-4143-8a64-2e422da65bcd';
var USE_AAD_TOKEN_PROVIDERS_CONFIGURABLE_KS = '88944eea-f19b-43fd-ac37-d46746038196';
var ENABLE_FOUNDATION_TELEMETRY_CONTEXT_FIELD_KS = 'ba3dbc95-59b7-41bf-a18c-125ad56f41cf';
/**
 * ExperimentalData key for whether to used the app id passed in the query string
 * @internal
 */
export var _INCLUDE_APP_WORKLOAD = 'includeAppWorkload';
/**
 * Query string key used for getting the value to appened to a workload
 * @internal
 */
export var _APP_WORKLOAD_QSP = 'app';
/**
 * Error message returned from the frame used to evaluate assemblies
 *
 * @internal
 */
export var EVAL_FRAME_FLAG = 'SuccessfulEvalFrame';
// QoS constants
var startQosScenarioName = 'SPComponentLoader.start';
var ecsCanaryClientFlightQosScenarioName = 'SPComponentLoader.ecsClientCanaryFlight';
var ecsCanaryClientFlight = 'SPOClientCanaryFlight';
// TODO : Remove this once the parity between ECS and FOR is achieved
var forCanaryClientFlight = 61851;
var forScenarioName = 'SPComponentLoader.forAndEcsCanaryFlightParity';
/**
 * Bootstrapper for the application
 * @internal
 */
var SPStarter = /** @class */ (function () {
    function SPStarter() {
    }
    /**
     * Takes an array of components included in the assembly. These components will be added to the component loader
     * during initialization, so they won't need to be loaded afterwards.
     * The array is of the form `[['id', 'version', { ... component ... }]]`
     *
     * @internal
     */
    SPStarter._initializeAssembly = function (assemblyComponents) {
        SPStarter._assemblyComponents = assemblyComponents;
        var bundledComponents = {};
        for (var _i = 0, assemblyComponents_1 = assemblyComponents; _i < assemblyComponents_1.length; _i++) {
            var assemblyComponent = assemblyComponents_1[_i];
            bundledComponents[assemblyComponent[0]] = assemblyComponent[2];
        }
        SPStarter._bundledComponents = bundledComponents;
    };
    /**
     * Raise the `onSecondFlush` event to resume starting with the full set of preloaded data.
     * This is called by the page scripts to resume loading the framework. Do not call it from your own code.
     *
     * @param preloadedData - Full set of preloaded data for the current session.
     */
    SPStarter.startSecondFlush = function (preloadedData) {
        _PerformanceLogger.setPerformanceProperty('SPStarter.startSecondFlush');
        window[COMPONENT_LOADER_GLOBAL_VARIABLE].registerManifests(preloadedData.manifests);
        _SPEventManager.instance.raiseStickyEvent('onSecondFlush', preloadedData);
    };
    /**
     * This is called by the page scripts to start loading the framework. Do not call it from your own code.
     * @param preloadedData - Data initialized on the page by the server.
     * @param handleFailure - Error handler function provided by the server to execute if there is SPFx can't be loaded.
     *  It may redirect to an error page or log error data in the console.
     * @param debugData - This parameter is used when the loader initializes a debug loader and
     *  should never be provided by any external callers.
     * @param isFirstFlush - Indicates whether the caller intends to call `startSecondFlush` to finish starting SPFx.
     */
    SPStarter.start = function (preloadedData, handleFailure, debugData, isFirstFlush) {
        var _this = this;
        var _a, _b;
        _PerformanceLogger.devMark('SPStarter.start');
        var _c = preloadedData.clientSideApplicationId, clientSideApplicationId = _c === void 0 ? '00000000-0000-0000-0000-000000000000' : _c, spPageContextInfo = preloadedData.spPageContextInfo, buildNumber = preloadedData.buildNumber;
        var correlationId = spPageContextInfo.CorrelationId;
        this.initializeFlightsAndKillswitches(spPageContextInfo);
        if (SPLoaderFlights._isErrorNautEnabled()) {
            // Patch console.log as early as possible in startup (after dev mark so we don't lose that) so we capture
            // any subsequent log noise that might help diagnose failures leading to error.aspx redirects.
            SPStarter._overrideTraceMethodsForErrorPage();
        }
        if (!overrideXMLHttpRequestKSActivated()) {
            overrideXMLHttpRequest();
        }
        if (SPLoaderFlights._isSafariIsolationEnabled()) {
            var redirectUrl = new URLSearchParams(window.location.search).get(REDIRECT_PARAM);
            if (redirectUrl) {
                window.location.href = decodeURIComponent(redirectUrl);
            }
            if (window.parent !== window) {
                window.parent.postMessage(APPLICATION_STARTED_DATA, '*');
            }
        }
        var clientSideApplicationManifest;
        if (!isPassClientSideApplicationManifestToRootScopeKSActivated()) {
            // trying to find the manifest
            clientSideApplicationManifest = preloadedData.manifests.find(function (manifest) { return manifest.id === clientSideApplicationId; });
        }
        var serviceScope = createRootScope(spPageContextInfo, clientSideApplicationManifest);
        ManifestStore.instance = new OldManifestStoreShim(serviceScope, spPageContextInfo.webAbsoluteUrl, buildNumber);
        // eslint-disable-next-line @typescript-eslint/no-require-imports
        var spLoader = require('../index');
        ResourceUrlChecker.useSecondaryCdn = SPStarter.useSecondaryCdn;
        var componentLoader = new SPComponentLoader({
            preloadedData: preloadedData,
            serviceScope: serviceScope,
            assemblyComponents: SPStarter._assemblyComponents,
            ctor: RequireLoader,
            debugData: debugData || {},
            useSecondaryCdn: SPStarter.useSecondaryCdn
        });
        var qosMonitor = undefined;
        try {
            // Initialize telemetry
            Log._initialize(new _LogHandler());
            SPStarter._initializeEnvironment(clientSideApplicationId);
            Session._initialize({
                clientSideApplicationId: clientSideApplicationId,
                applicationId: correlationId
            });
            SPStarter._initializeTelemetry(preloadedData);
            _PerformanceLogger.markSpLoaderStart();
            qosMonitor = new _QosMonitor(startQosScenarioName);
            // Set locale
            LocaleStore.setLocale(spPageContextInfo.currentUICultureName);
            SPStarter._monitorClientFlight(ecsCanaryClientFlightQosScenarioName, ecsCanaryClientFlight, spPageContextInfo.ECSClientFeatures, (_b = (_a = spPageContextInfo.ExpFeatures) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0);
            // Initialize the proxies
            componentLoader._initialize(preloadedData, SPStarter._assemblyComponents, debugData || {});
            spLoader.SPComponentLoader._initialize(componentLoader);
            window[COMPONENT_LOADER_GLOBAL_VARIABLE] = spLoader.SPComponentLoader;
        }
        catch (error) {
            if (!_SPKillSwitch.isActivated(EARLY_QOS_KS /* 10/31/2023, early create of QosMonitor */)) {
                qosMonitor === null || qosMonitor === void 0 ? void 0 : qosMonitor.writeUnexpectedFailure('StartApplication', error);
            }
            return Promise.reject(error);
        }
        return DebugManager.initializeAsync(componentLoader, debugData)
            .then(function (debugLoadResult) {
            if (debugLoadResult.debugLoader) {
                // The debug loader will initialize telemetry handlers. To prevent double logging, we skip them in this case.
                if (!_SPKillSwitch.isActivated('49c783a0-5b5e-4b25-972b-daa087fadb73' /* 09/11/24, finnnavin: prevent main loader from handling telemetry when debug is enabled */)) {
                    SPStarter._isTelemetryDisabled = true;
                }
                else {
                    _Telemetry.haltBackend();
                }
                // If we're loading a debug loader, call "start" on it instead of continuing with the normal initialization
                // flow
                componentLoader._unloadComponents();
                return debugLoadResult.debugLoader.start(preloadedData, handleFailure, debugLoadResult, isFirstFlush);
            }
            if (!_SPKillSwitch.isActivated(DISABLE_TOKEN_FETCH_KS)) {
                try {
                    var _a = preloadedData.spPageContextInfo, aadInstanceUrl = _a.aadInstanceUrl, aadSessionId = _a.aadSessionId, aadTenantId = _a.aadTenantId, aadUserId = _a.aadUserId, spRequestGuid = _a.CorrelationId, userPrincipalName = _a.userPrincipalName, isAnonymousGuestUser = _a.isAnonymousGuestUser, isExternalGuestUser = _a.isExternalGuestUser, isEmailAuthenticationGuestUser = _a.isEmailAuthenticationGuestUser;
                    if (!isAnonymousGuestUser &&
                        (_SPKillSwitch.isActivated(DO_NOT_ENSURE_GUEST_USERS_KS) ||
                            (!isExternalGuestUser && !isEmailAuthenticationGuestUser))) {
                        if (!_SPKillSwitch.isActivated(USE_AAD_TOKEN_PROVIDERS_CONFIGURABLE_KS)) {
                            var aadTokenProvider = new AadTokenProvider(_AadTokenProviders.preAuthorizedConfiguration);
                            void aadTokenProvider._ensureState();
                        }
                        else {
                            var aadTokenProvider = new AadTokenProvider({
                                aadInstanceUrl: aadInstanceUrl,
                                aadSessionId: aadSessionId,
                                aadTenantId: aadTenantId,
                                aadUserId: aadUserId,
                                redirectUri: "".concat(window.location.origin, "/_forms/").concat(_AadConstants.SPFX_SINGLE_SIGN_ON_REPLY_URL),
                                servicePrincipalId: _AadConstants.PRE_AUTHORIZED_APP_PRINCIPAL_ID,
                                spRequestGuid: spRequestGuid,
                                thirdPartyReplyUrisUpdated: true,
                                userPrincipalName: userPrincipalName
                            });
                            void aadTokenProvider._ensureState();
                        }
                    }
                }
                catch (e) {
                    // Ignore
                }
            }
            // Load the dev tools after the debug loader to reference the right diagnostics component
            developerToolsLoader.initialize(serviceScope);
            // Only run the experiment if the tiny loader is running
            if (SPLoaderFlights._isOptimisticPreloadEnabled()) {
                spLoader.SPComponentLoader._preloadComponents();
            }
            if (SPLoaderFlights._isMinimalStartFlightEnabled() && !isFirstFlush) {
                _SPEventManager.instance.raiseStickyEvent('onSecondFlush', preloadedData);
            }
            var status = _SPExperiment.getVariantAndLogExposure(90033 /* PagesClientServerRequestOptimzationExperiment */) === 1;
            _PerformanceLogger.trackExperiment(90033, status ? 'Treatment' : 'Control');
            var ecsPerformanceTracking = _SPExperiment.getVariantAndLogExposure(90215 /* ECSSupportEnabledInSPOExperiment */) === 1;
            _PerformanceLogger.trackExperiment(90215, ecsPerformanceTracking ? 'Treatment' : 'Control');
            // Start the application
            return spLoader.SPComponentLoader._startApplication(preloadedData);
        })
            .then(function (application) {
            qosMonitor.writeSuccess();
            // Log telemetry data from buffers to the server
            SPStarter._logDataInRealTime();
            return application;
        })
            .catch(function (error) {
            if (error.message === EVAL_FRAME_FLAG) {
                return {};
            }
            if (qosMonitor) {
                if (error.message === 'Out of stack space') {
                    qosMonitor.writeExpectedFailure('ConflictingPolyfill', error);
                }
                else {
                    qosMonitor.writeUnexpectedFailure('StartApplication', error);
                }
            }
            SPStarter._logDataInRealTime();
            return _this._handleError(error, strings.loaderUserFriendlyError, _Telemetry.isInitialized ? 'ClientError' : 'ClientErrorBeforeTelemetry', correlationId, preloadedData);
        });
    };
    /**
     * Initializes the flights and killswitches.  This should only ever be called during page load.  It is broken out
     * so that we can initialize the killswitches before trying to do any work as that allows killswitches and flights
     * to be used before start is run.
     *
     * @internal
     */
    SPStarter.initializeFlightsAndKillswitches = function (spPageContextInfo) {
        var _a, _b;
        // We can't put a killswitch on the new code so we just check a static boolean to ensure we only do this once
        if (!this._isKillSwitchAndFlightingInitialized) {
            this._isKillSwitchAndFlightingInitialized = true;
            // Initialize flights and killswitches with the data from the server
            _SPFlight.initialize(spPageContextInfo.ExpFeatures);
            _SPKillSwitch.initialize(spPageContextInfo.killSwitches);
            _SPFlight.initializeEcs(spPageContextInfo.ECSClientFeatures);
            // Initialize debug flights and killswitches from the query params and cookies
            var allowDebug = _SPFlight.isDebugFlightEnabled || Environment.type === EnvironmentType.Local;
            if (allowDebug) {
                if (!_SPFlight.isEnabled(REFACTOR_DEBUG_FLIGHTS_AND_KILLSWITCHES_FLIGHT_ID)) {
                    var debugFlightsQueryParam = this._getQueryParamValue(DEBUG_FLIGHTS_QUERY_PARAM);
                    var enableFeaturesQueryParam = this._getQueryParamValue(ENABLE_FEATURES_QUERY_PARAM);
                    var disableFeaturesQueryParam = this._getQueryParamValue(DISABLE_FEATURES_QUERY_PARAM);
                    var enableFeaturesCookieValue = this._getCookieValue(ENABLE_FEATURES_COOKIE_NAME);
                    var disableFeaturesCookieValue = this._getCookieValue(DISABLE_FEATURES_COOKIE_NAME);
                    var debugFlights = new Set([].concat((debugFlightsQueryParam === null || debugFlightsQueryParam === void 0 ? void 0 : debugFlightsQueryParam.split(',')) || [], (enableFeaturesQueryParam === null || enableFeaturesQueryParam === void 0 ? void 0 : enableFeaturesQueryParam.split(',')) || [], (enableFeaturesCookieValue === null || enableFeaturesCookieValue === void 0 ? void 0 : enableFeaturesCookieValue.split(',')) || [], ((_a = disableFeaturesQueryParam === null || disableFeaturesQueryParam === void 0 ? void 0 : disableFeaturesQueryParam.split(',')) === null || _a === void 0 ? void 0 : _a.map(function (f) { return "!".concat(f); })) || [], ((_b = disableFeaturesCookieValue === null || disableFeaturesCookieValue === void 0 ? void 0 : disableFeaturesCookieValue.split(',')) === null || _b === void 0 ? void 0 : _b.map(function (f) { return "!".concat(f); })) || []));
                    var debugFlightsStr = debugFlights.size
                        ? Array.from(debugFlights).join(',')
                        : undefined;
                    _SPFlight.setDebugFlights(debugFlightsStr);
                }
                _SPKillSwitch.setDebugKillswitches(this._getQueryParamValue(DEBUG_KILLSWITCHES_QUERY_PARAM));
            }
        }
    };
    /**
     * Installs a lightweight wrapper around telemetry that appends messages to an in‑memory buffer.
     * The buffer is later written to sessionStorage when redirecting to errorNaut.aspx so diagnostics
     * can be surfaced there. This is intentionally minimal to avoid perf regressions.
     */
    SPStarter._overrideTraceMethodsForErrorPage = function () {
        if (SPStarter._isTracePatched) {
            return; // already patched
        }
        try {
            // eslint-disable-next-line no-console
            SPStarter._originalConsoleLog = console.log.bind(console);
            // eslint-disable-next-line no-console
            SPStarter._originalConsoleError = console.error.bind(console);
            // eslint-disable-next-line no-console
            console.log = SPStarter._overridenConsoleLogMethod;
            // eslint-disable-next-line no-console
            console.error = SPStarter._overridenConsoleErrorMethod;
            SPStarter._isTracePatched = true;
        }
        catch (_a) {
            // If anything goes wrong we silently skip patching to avoid impacting startup
        }
    };
    SPStarter._overridenConsoleLogMethod = function () {
        var data = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            data[_i] = arguments[_i];
        }
        SPStarter._setTraceData.apply(SPStarter, __spreadArray([SPStarter._consoleLogBuffer, SPStarter._originalConsoleLog], data, false));
    };
    SPStarter._overridenConsoleErrorMethod = function () {
        var data = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            data[_i] = arguments[_i];
        }
        SPStarter._setTraceData.apply(SPStarter, __spreadArray([SPStarter._consoleErrorBuffer, SPStarter._originalConsoleError], data, false));
        // Always capture a stack trace when writing to console as that is part of the
        // actual browsers console.error behavior
        try {
            throw new Error();
        }
        catch (e) {
            SPStarter._consoleErrorBuffer.push(e.stack || '');
        }
    };
    SPStarter._setTraceData = function (inMemoryTrace, traceFunction) {
        var args = [];
        for (var _i = 2; _i < arguments.length; _i++) {
            args[_i - 2] = arguments[_i];
        }
        try {
            var serialized = args
                .map(function (a) {
                try {
                    if (typeof a === 'string') {
                        return a;
                    }
                    if (a instanceof Error) {
                        return "".concat(a.name, ": ").concat(a.message);
                    }
                    return JSON.stringify(a);
                }
                catch (_a) {
                    return '[Unserializable]';
                }
            })
                .join(' ');
            // Cap individual entry length to avoid unbounded growth
            var entry = "".concat(serialized).slice(0, 4000);
            inMemoryTrace.push(entry);
            // Soft cap total buffer size (approx). Drop oldest when exceeding.
            if (inMemoryTrace.length > 500) {
                inMemoryTrace.splice(0, inMemoryTrace.length - 500);
            }
        }
        catch (_a) {
            // swallow
        }
        try {
            traceFunction === null || traceFunction === void 0 ? void 0 : traceFunction.apply(void 0, args);
        }
        catch (_b) {
            // swallow any issues calling original
        }
    };
    /** Writes buffered console logs to sessionStorage (best effort). */
    SPStarter._persistConsoleLogsForErrorPage = function (errorInformation) {
        try {
            if (SPStarter._consoleLogBuffer.length) {
                var logPayload = JSON.stringify({ v: 1, logs: SPStarter._consoleLogBuffer });
                window.sessionStorage.setItem(SPStarter._SESSION_STORAGE_LOG_KEY, logPayload);
            }
            if (SPStarter._consoleErrorBuffer.length) {
                var errorPayload = JSON.stringify({ v: 1, logs: SPStarter._consoleErrorBuffer });
                window.sessionStorage.setItem(SPStarter._SESSION_STORAGE_ERROR_KEY, errorPayload);
                var errorInfoPayload = JSON.stringify({ v: 1, info: errorInformation });
                window.sessionStorage.setItem(SPStarter._SESSION_STORAGE_ERROR_INFO_KEY, errorInfoPayload);
            }
        }
        catch (_a) {
            // ignore
        }
    };
    SPStarter._logDataInRealTime = function () {
        if (!SPStarter._isTelemetryDisabled && !SPStarter._isTelemetryLoggingInRealTime) {
            _Telemetry.startRealTimeProcessing();
            SPStarter._isTelemetryLoggingInRealTime = true;
        }
    };
    SPStarter._initializeEnvironment = function (clientSideApplicationId) {
        var type;
        if (window.location.hostname === 'localhost' ||
            (window.ENVIRONMENTTYPE && window.ENVIRONMENTTYPE === 'Local')) {
            type = EnvironmentType.Local;
        }
        else if (clientSideApplicationId === 'eb4b666b-5c29-4dad-9a99-23613f21a2b7') {
            // 'eb4b666b-5c29-4dad-9a99-23613f21a2b7' is the classic page application id.
            type = EnvironmentType.ClassicSharePoint;
        }
        else {
            type = EnvironmentType.SharePoint;
        }
        Environment._initialize({ type: type });
    };
    SPStarter._initializeTelemetry = function (preloadedData) {
        var _a;
        var buildNumber = preloadedData.buildNumber, clientSideApplicationId = preloadedData.clientSideApplicationId, listItemId = preloadedData.listItemId, manifests = preloadedData.manifests, spPageContextInfo = preloadedData.spPageContextInfo;
        var CorrelationId = spPageContextInfo.CorrelationId, listId = spPageContextInfo.listId, siteId = spPageContextInfo.siteId, webId = spPageContextInfo.webId;
        var appManifest = manifests[0];
        if (!isFindAppManifestInsteadUsingFirstKSActivated()) {
            var foundManifest = manifests.find(function (manifest) { return manifest.id === clientSideApplicationId; });
            if (foundManifest) {
                appManifest = foundManifest;
            }
        }
        var workload = appManifest.alias;
        if (!overrideWorkloadKSActivated() &&
            appManifest.experimentalData &&
            appManifest.experimentalData[_INCLUDE_APP_WORKLOAD]) {
            try {
                var locationUrl = new URL(location.href);
                if (locationUrl.searchParams.get(_APP_WORKLOAD_QSP)) {
                    workload += '-' + locationUrl.searchParams.get(_APP_WORKLOAD_QSP);
                }
            }
            catch (_b) {
                // No-op as workload should be left as the default value
            }
        }
        var isFoundation = undefined;
        if (!_SPKillSwitch.isActivated(ENABLE_FOUNDATION_TELEMETRY_CONTEXT_FIELD_KS /* 1/29/2025, lmarulanda: add isFoundation telemetry context field */)) {
            // isFoundation will be included in the minimal preloaded data
            isFoundation = (_a = preloadedData.isFoundation) !== null && _a !== void 0 ? _a : false;
        }
        // The first manifest is always the application's manifest.
        _Telemetry.initialize(workload, spPageContextInfo, buildNumber, clientSideApplicationId, isFoundation);
        _Diagnostics.initialize({
            enableConsoleLog: SPStarter._isConsoleLogEnabled(),
            siteId: siteId,
            webId: webId,
            listId: listId,
            listItemUniqueId: listItemId,
            correlationId: CorrelationId
        });
    };
    SPStarter._isQueryParameterTrue = function (name) {
        var parameterValue;
        try {
            parameterValue = this._getQueryParamValue(name) === 'true';
        }
        catch (error) {
            parameterValue = false;
        }
        return parameterValue;
    };
    SPStarter._isConsoleLogEnabled = function () {
        return SPStarter._isQueryParameterTrue('enableConsoleLog');
    };
    SPStarter._handleError = function (error, userFriendlyMessage, operationName, correlationId, preloadedData) {
        var errorInformation = {
            message: userFriendlyMessage,
            correlationId: correlationId,
            error: error,
            operationName: operationName
        };
        if (this._isRedirectDisabled(preloadedData)) {
            this._consoleErrorHandleFailure(errorInformation);
        }
        else {
            if (SPLoaderFlights._isErrorNautEnabled()) {
                // Persist buffered console logs to sessionStorage for retrieval on error.aspx
                this._persistConsoleLogsForErrorPage(errorInformation);
                this._errornautAspxHandleFailure(preloadedData);
                return Promise.resolve({});
            }
            else {
                this._errorAspxHandleFailure(errorInformation, preloadedData);
            }
        }
        return Promise.reject(error);
    };
    SPStarter._isRedirectDisabled = function (preloadedData) {
        return (SPStarter._isQueryParameterTrue('noredir') ||
            Environment.type === EnvironmentType.ClassicSharePoint || // Classic pages
            Environment.type === EnvironmentType.Local || // Local workbench
            preloadedData.clientSideApplicationId === componentConstants.listViewHostComponentId); // ListView application
    };
    SPStarter._consoleErrorHandleFailure = function (errorInformation) {
        // eslint-disable-next-line no-console
        console.error(errorInformation.message);
        if (errorInformation.correlationId) {
            // eslint-disable-next-line no-console
            console.error("Correlation Id: " + errorInformation.correlationId);
        }
        if (errorInformation.operationName) {
            // eslint-disable-next-line no-console
            console.error("Operation name: " + errorInformation.operationName);
        }
        if (errorInformation.error) {
            // eslint-disable-next-line no-console
            console.error(errorInformation.error.message);
            // eslint-disable-next-line no-console
            console.error("CALL STACK: " + errorInformation.error.stack);
        }
    };
    SPStarter._errornautAspxHandleFailure = function (preloadedData) {
        var _a;
        window.location.href =
            (((_a = preloadedData.spPageContextInfo) === null || _a === void 0 ? void 0 : _a.webAbsoluteUrl) || '') +
                '/_layouts/15/spfxplayground.aspx' +
                '?prefabAppId=8e0f6d12-1d1d-4067-9f2b-3b23a3d26f48';
    };
    SPStarter._errorAspxHandleFailure = function (errorInformation, preloadedData) {
        var _a;
        window.location.href =
            (((_a = preloadedData.spPageContextInfo) === null || _a === void 0 ? void 0 : _a.webAbsoluteUrl) || '') +
                '/_layouts/15/error.aspx' +
                '?ErrorCorrelationId=' +
                encodeURIComponent(errorInformation.correlationId || '') +
                '&ErrorText=' +
                encodeURIComponent(errorInformation.message) +
                '&ErrorDetails=' +
                encodeURIComponent(errorInformation.error ? errorInformation.error.toString() : '') +
                '&Name=' +
                encodeURIComponent(errorInformation.operationName || 'DefaultOperation') +
                '&ErrorCategory=spfx';
    };
    SPStarter._getQueryParamValue = function (name) {
        if (!this._queryParams) {
            this._queryParams = new URLSearchParams(self.location.search);
        }
        if (this._queryParams.has(name)) {
            return this._queryParams.get(name);
        } // else returns undefined
    };
    // delete this when graduating 61583 flight
    SPStarter._getCookieValue = function (name) {
        var cookieMatch = self.document.cookie.match("(^|;)\\s*".concat(name, "\\s*=\\s*([^;]+)"));
        return cookieMatch ? cookieMatch.pop() : undefined;
    };
    // method to monitor the client flight
    SPStarter._monitorClientFlight = function (qosMonitorScenarioName, flightName, ecsClientFeatures, expFeaturesLength) {
        var qosMonitor = new _QosMonitor(qosMonitorScenarioName);
        var isClientFlightEnabled = _SPFlight.isEcsFlightEnabled(flightName);
        if (!isClientFlightEnabled) {
            qosMonitor.writeUnexpectedFailure(flightName, new Error("[ECS] ".concat(flightName, " is disabled. Value of ECSClientFeatures: ").concat(ecsClientFeatures, ", ExpFeatures length: ").concat(expFeaturesLength, " at this point in time.")));
        }
        else {
            qosMonitor.writeSuccess();
        }
        //TODO: Remove this once the parity between ECS and FOR is achieved
        if (flightName === ecsCanaryClientFlight) {
            this._checkForClientFlightParity(isClientFlightEnabled);
        }
    };
    // method to check the parity between ECS and FOR client flight
    SPStarter._checkForClientFlightParity = function (isEcsClientFlightEnabled) {
        var isParity = false;
        var forCanaryQosMonitor = new _QosMonitor(forScenarioName);
        try {
            var isForClientFlightEnabled = _SPFlight.isEnabled(forCanaryClientFlight);
            isParity = isEcsClientFlightEnabled === isForClientFlightEnabled;
        }
        catch (error) {
            //ignore error
        }
        if (!isParity) {
            forCanaryQosMonitor.writeUnexpectedFailure('ECSAndFORCanaryClientFlightParity', new Error("ECS and FOR canary client flight status are not in parity"));
        }
        else {
            forCanaryQosMonitor.writeSuccess();
        }
    };
    /**
     * Value indicating if we should load scripts from the secondary CDN.
     */
    SPStarter.useSecondaryCdn = false;
    SPStarter._isTelemetryDisabled = false;
    SPStarter._isTelemetryLoggingInRealTime = false;
    SPStarter._isKillSwitchAndFlightingInitialized = false;
    /**
     * Buffer of console log entries captured during the lifetime of the page. Each entry is a tuple of
     * timestamp (ISO string) and the arguments passed to console.log serialized best-effort.
     */
    SPStarter._consoleLogBuffer = [];
    SPStarter._consoleErrorBuffer = [];
    /** Preserve the original console.log implementation so we can continue to emit to the real console. */
    SPStarter._originalConsoleLog = undefined;
    /** Preserve the original console.error implementation so we can continue to emit to the real console. */
    SPStarter._originalConsoleError = undefined;
    /** Whether we've already patched console.log */
    SPStarter._isTracePatched = false;
    /** SessionStorage key used when persisting buffered console logs for retrieval on error.aspx */
    SPStarter._SESSION_STORAGE_LOG_KEY = 'SPFx.Log.Buffer';
    SPStarter._SESSION_STORAGE_ERROR_KEY = 'SPFx.Error.Buffer';
    SPStarter._SESSION_STORAGE_ERROR_INFO_KEY = 'SPFx.Error.Information';
    return SPStarter;
}());
export default SPStarter;
//# sourceMappingURL=SPStarter.js.map